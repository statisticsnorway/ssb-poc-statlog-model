{
    "$id": "https://ssb.no/schemas/ssb-poc-statlog-model/quality-control-description/2.0.0",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "Quality control description",
    "description": "Model for description of quality controls used in a statistical production.",
    "type": "object",
    "properties": {
        "schema_version": {
            "description": "Version of this schema.",
            "type": "string",
            "const": "2.0.0"
        },
        "quality_control_id": {
            "description": "A unique quality control ID",
            "type": "string"
        },
        "quality_control_description": {
            "description": "Quality control description",
            "type": "string"
        },
        "quality_control_type": {
            "description": "Quality control type: hard (H), soft (S), informative (I).",
            "type": "string",
            "enum": [
                "H",
                "S",
                "I"
            ]
        },
        "variables": {
            "description": "A description of which variables must be included in the quality control.",
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "variable_description": {
                        "type": "string"
                    }
                }
            }
        }
    },
    "required": [
        "schema_version",
        "quality_control_id",
        "quality_control_description",
        "quality_control_type",
        "variables"
    ]
}
